#include <stdio.h>
int add(int a, int b)
{
    return a + b;
}
int main()
{
    int result = add(5, 3);
    printf("Sum: %d\n", result);
    return 0;
}
// // This program defines a function to add two integers and returns the result.
// // The main function calls the add function with two numbers and prints the sum to the console.      